When we return from an function and we didnot use 
# console.log(function call())
then we get undefined so when we return something from a function always use
# console.log(function call());
# console.log() by default return value is undefined